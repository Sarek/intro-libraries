clean:
	pushd part1 && $(MAKE) clean && popd
	pushd part2 && $(MAKE) clean && popd
	pushd part3 && $(MAKE) clean && popd
	pushd part3b && $(MAKE) clean && popd
	pushd part4 && $(MAKE) clean && popd
	pushd part5 && $(MAKE) clean && popd
	pushd part6 && $(MAKE) clean && popd

